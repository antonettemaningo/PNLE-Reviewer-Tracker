# Setup

Open `index.html` in a modern browser.

The app remains an offline-first single-page HTML application. The supplied V3 Full source is preserved separately in `PNLE_Command_Center_V3_Full_ORIGINAL.html`.
