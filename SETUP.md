# Setup

Open `index.html` in a modern browser.

The app remains an offline-first single-page HTML application. The first screen asks the student to sign in or create a local account; guest mode is also available.

`PNLE_Command_Center_V3_Full_ORIGINAL.html` is the untouched original supplied V3 build.
