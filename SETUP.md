# Setup

Open `index.html` in a modern browser.

The app remains offline-first. Local accounts use the existing browser storage/password hashing system. Optional cloud sync remains available through the existing settings.

The `assets/` and `data/` folders are reserved for future extraction of visual assets, sounds, and data from the self-contained HTML build.
