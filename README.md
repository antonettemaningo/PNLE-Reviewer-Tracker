# PNLE Review — Real Backend

Real Supabase authentication + Postgres per-user study state with Row Level Security.

- `index.html` — GitHub Pages frontend
- `PNLE_Command_Center_V3_Full.html` — updated V3 build
- `PNLE_Command_Center_V3_Full_ORIGINAL.html` — untouched original backup
- `supabase/schema.sql` — database/RLS schema
- `supabase/SETUP.md` — configuration steps

The frontend contains only the Supabase public anon key after configuration. Never expose the service-role key.
